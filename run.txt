# STEP 0 (optional but recommended)
# Create and activate virtual environment
python -m venv venv
source venv/bin/activate      # Linux / Mac
venv\Scripts\activate         # Windows

# STEP 1
# Install dependencies
pip install -r requirements.txt

# STEP 2
# Fetch raw weather data from APIs
python src/data_fetch.py

# STEP 3
# Preprocess, compute API weights, fuse data, engineer features
python src/preprocess.py

# STEP 4
# Train the LSTM model on fused data
python src/train.py

# STEP 5
# Generate predictions and farmer advisory
python src/predict_and_advisory.py

# STEP 6 (optional – production mode)
# Run full pipeline every hour
python src/scheduler.py
